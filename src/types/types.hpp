#ifndef TYPES_H
#define TYPES_H

#include <cstdint>
#include <string>
#include "utils/constants.hpp"

using GameObjectId   = std::uint32_t;
using TagName        = std::string;
using GroupName      = std::string;
using AllegianceName = std::string;
using Pixels         = std::int32_t;
using FontSize       = constants::Fonts::Size;

#endif
